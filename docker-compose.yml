services:
  db:
    image: postgres
    container_name: readstack_db
    restart: always
    ports:
      - "54323:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: postgres